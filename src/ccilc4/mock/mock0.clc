
def maxn : nat -> nat -> nat
  | x y =>
    match x, y with
    | zero, _ => y
    | _, zero => x
    | succ x, succ y => succ (maxn x y)

def one := succ zero

def two := succ (succ zero)

def foo := maxn one

def bar := (foo two)